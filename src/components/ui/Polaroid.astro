---
type Props = {
  img: string;
  cls?: string;
};

const { img, cls } = Astro.props;
---
<div class={"bg-white aspect-square p-4 pb-16 lg:p-6 lg:pb-32 " + (cls ? ` ${cls}` : '')}>
<img src={img} class="w-full" alt="Polaroid" loading="lazy" fetchpriority="low" decoding="async">
</div>
